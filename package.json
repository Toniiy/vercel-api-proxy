{
  "name": "oebb-vercel-proxy",
  "version": "1.0.0",
  "description": "ÖBB train data proxy for Vercel serverless",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build needed for serverless'"
  },
  "dependencies": {
    "axios": "^1.6.2"
  },
  "engines": {
    "node": "18.x"
  }
}